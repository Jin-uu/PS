#include <iostream>

using ll = long long;

int main(void) {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);
  std::cout.tie(nullptr);

  ll A, B, C;
  std::cin >> A >> B >> C;
  std::cout << A + B + C;
}